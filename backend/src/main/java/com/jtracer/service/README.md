# Service Implementations (Private)

Persistence and coordinator implementations (`*ServiceImpl`, `CollectorCoordinatorServiceImpl`)
are **not included in the public repository**.

Public consumers should refer to service interfaces and `docs/SYSTEM_DESIGN.md` for behavior contracts.
